# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Disable border
hide_edge_borders both

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font pango:Roboto Light, FontAwesome 9
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:Roboto 10, FontAwesome 10

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec terminology

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+l move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+h move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
#Workspaces
set $firstws 1: 
set $secondws 2: 
set $thirdws 3: 
set $fourthws 4: 
set $fifthws 5: 
set $sixthws 6: 
set $seventhws 7:
set $eighthws 8:
set $ninethws 9: 
set $tenthws 10:
# switch to workspace
bindsym $mod+aogonek workspace $firstws
bindsym $mod+ccaron workspace $secondws
bindsym $mod+eogonek workspace $thirdws
bindsym $mod+eabovedot workspace $fourthws
bindsym $mod+iogonek workspace $fifthws
bindsym $mod+scaron workspace $sixthws
bindsym $mod+uogonek workspace $seventhws
bindsym $mod+umacron workspace $eighthws
bindsym $mod+doublelowquotemark workspace $ninethws
bindsym $mod+leftdoublequotemark workspace $tenthws

bindsym $mod+1 workspace $firstws
bindsym $mod+2 workspace $secondws
bindsym $mod+3 workspace $thirdws
bindsym $mod+4 workspace $fourthws
bindsym $mod+5 workspace $fifthws
bindsym $mod+6 workspace $sixthws
bindsym $mod+7 workspace $seventhws
bindsym $mod+8 workspace $eighthws
bindsym $mod+9 workspace $ninethws
bindsym $mod+0 workspace $tenthws

# move focused container to workspace
bindsym $mod+Shift+aogonek move container to workspace $firstws
bindsym $mod+Shift+ccaron move container to workspace $secondws
bindsym $mod+Shift+eogonek move container to workspace $thirdws
bindsym $mod+Shift+eabovedot move container to workspace $fourthws
bindsym $mod+Shift+iogonek move container to workspace $fifthws
bindsym $mod+Shift+scaron move container to workspace $sixthws
bindsym $mod+Shift+uogonek move container to workspace $seventhws
bindsym $mod+Shift+umacron move container to workspace $eighthws
bindsym $mod+Shift+doublelowquotemark move container to workspace $ninethws
bindsym $mod+Shift+leftdoublequotemark move container to workspace $tenthws

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $firstws
bindsym $mod+Shift+2 move container to workspace $secondws
bindsym $mod+Shift+3 move container to workspace $thirdws
bindsym $mod+Shift+4 move container to workspace $fourthws
bindsym $mod+Shift+5 move container to workspace $fifthws
bindsym $mod+Shift+6 move container to workspace $sixthws
bindsym $mod+Shift+7 move container to workspace $seventhws
bindsym $mod+Shift+8 move container to workspace $eighthws
bindsym $mod+Shift+9 move container to workspace $ninethws
bindsym $mod+Shift+0 move container to workspace $tenthws
#Window bindings
for_window [class="Spotify$"] move to workspace $fourthws
for_window [title="TermCal"] floating enable border pixel 1 sticky enable
for_window [class="chromium-browser"] border pixel 0
# Startup
exec feh --bg-scale /home/domas/Pictures/wallpaper.png
exec --no-startup-id compton --config ~/.config/compton.conf -b
exec --no-startup-id i3-msg 'workspace 1: ; exec chromium-browser'
exec --no-startup-id i3-msg 'workspace 3: ; exec firefox'
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 2 px or 2 ppt
        bindsym Down resize grow height 2 px or 2 ppt
        bindsym Up resize shrink height 2 px or 2 ppt
        bindsym Right resize grow width 2 px or 2 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Language swich
set $langmode Language: (a) - en, (s) - lt
mode "$langmode" {
      bindsym a exec setxkbmap -layout us, mode "default"
      bindsym s exec setxkbmap -layout lt, mode "default"
      bindsym Return mode "default"
      bindsym Escape mode "default"
}

#Language switch
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec i3lock -i /home/domas/Pictures/wallpaper.png, mode "default"
    bindsym e exec  i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym h exec --no-startup-id systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
#Key bindings
bindsym $mod+Shift+s mode "$mode_system"

bindsym $mod+z mode "$langmode"
bindsym F1 exec terminology -H -e cat /home/domas/help.txt
bindsym F2 exec terminology -e vim /home/domas/.config/i3/config
bindsym $mod+r mode "resize"
bindsym Pause exec i3lock -i /home/domas/Pictures/wallpaper.png
bindsym F3 exec i3-msg 'append_layout /home/domas/.i3/workspace-prog.json', exec chromium-browser, exec atom -T Editor, exec terminology -T SSH -e ssh domas@192.168.1.3, exec terminology -T Terminal
bindsym $mod+F3 exec bash -c "sshfs domas@192.168.1.3:/home/domas/ /home/domas/rpi" 

# scrot & gimp - root
bindsym Print exec scrot -e 'mv $f /tmp/ && gimp /tmp/$f'
# scrot & gimp - select window or rectangle
bindsym Mod1+Print exec scrot -s -e 'mv $f /tmp/ && gimp /tmp/$f'



# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command py3status -c ~/.config/i3/i3status.conf
        tray_output primary
	position top
#	fonts xft:Roboto, FontAwesome 9
        colors {
        statusline	#999999



    }
}
